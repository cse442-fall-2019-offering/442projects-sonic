import { PanGestureHandler as BasePanGestureHandler } from 'react-native-gesture-handler';
import { createAnimatedComponent } from '..//react-native-reanimated/createAnimatedComponent';
export const PanGestureHandler = createAnimatedComponent(BasePanGestureHandler, (inputProps) => {
    const props = {};
    for (const key of Object.keys(inputProps)) {
        const value = inputProps[key];
        if (key !== 'enabled') {
            props[key] = value;
        }
    }
    return props;
});
