import React from 'react';
export const RouterContext = React.createContext(undefined);
export const RouterProvider = ({ router, children }) => <RouterContext.Provider value={router} children={children}/>;
