export function measure(element, width) {
    setTimeout(() => {
        if (element.current) {
            element.current.measure((_x, _y, w) => {
                width.setValue(w);
            });
        }
    });
}
