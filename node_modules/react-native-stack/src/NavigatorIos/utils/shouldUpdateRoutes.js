export function shouldUpdateRoutes(matchingRoutes, entries) {
    const keys = {};
    for (const route of matchingRoutes) {
        keys[route.location.key] = true;
    }
    for (const route of entries) {
        if (typeof keys[route.key] === 'undefined') {
            return true;
        }
    }
    return false;
}
