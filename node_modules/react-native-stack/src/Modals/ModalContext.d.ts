import React from 'react';
import { ILocation, IMatch, IRoute, LocationState, MatchParams } from '../Router';
export declare type Modal<S = LocationState, P = MatchParams, R = {}> = {
    remove: () => void;
} & ILocation<S> & IMatch<P> & R;
export declare const ModalContext: React.Context<Modal<any, MatchParams, {}> | null | undefined>;
export declare type ModalProviderProps = {
    location: ILocation;
    match: IMatch;
    route: IRoute;
};
export declare const ModalProvider: React.FC<ModalProviderProps>;
